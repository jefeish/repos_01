---
conditions:
    all:
    - fact: payload.issue.state
      operator: includes
      value: closed
    # Prevent loops
    - fact: payload.sender.type
      operator: notEqual
      value: Bot
event:
    type: issuesRemoveLabels
    params:
        data:
        - Blocked
        - To do
        - Backlog
