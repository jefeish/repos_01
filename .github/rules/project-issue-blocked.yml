---
conditions:
  all:
    - fact: payload.issue.labels.name
      operator: contains
      value: Blocked

event:
  type: assignIssueToProject
  params:
    data:
      - Kanban2
      - Blocked

# default is '1'
priority: 1
