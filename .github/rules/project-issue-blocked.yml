---
# default is '1'
priority: 10
name: "project 'blocked'' rule"

conditions:
  all:
    - fact: payload.issue.labels.name
      operator: contains
      value: Blocked
    - fact: payload.issue.state
      operator: notEqual
      value: closed

event:
  type: assignIssueToProject
  params:
    data:
      - Kanban2
      - Blocked
