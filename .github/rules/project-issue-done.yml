---
# default is '1'
priority: 10
name: "project 'done' rule"

conditions:
  all:
    - fact: payload.issue.state
      operator: equal
      value: closed

event:
  type: assignIssueToProject
  params:
    data:
      - Kanban2
      - Done
